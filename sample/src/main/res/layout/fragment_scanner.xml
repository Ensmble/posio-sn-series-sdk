<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView style="@style/SectionHeader" android:text="Camera scanner"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="8dp"
            android:text="Opens the camera to scan a QR code or barcode. Works on any device with a camera."/>

        <Button android:id="@+id/btnCameraScan" style="@style/Cell"
            android:text="Scan with camera (QR / barcode)"/>

        <TextView style="@style/SectionHeader" android:text="Infrared scanner"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="12dp"
            android:text="Uses the device's built-in hardware scan engine. Press the device's physical scan button, or tap Trigger, then point it at a barcode — there is no on-screen preview."/>

        <Button android:id="@+id/btnTrigger" style="@style/Cell" android:text="Trigger scan"/>
        <Button android:id="@+id/btnStop" style="@style/Cell" android:text="Stop scan"/>

        <TextView style="@style/SectionHeader" android:text="Last scan"/>

        <TextView
            android:id="@+id/scanResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textIsSelectable="true"
            android:text="(waiting for a scan)"/>

    </LinearLayout>
</ScrollView>
