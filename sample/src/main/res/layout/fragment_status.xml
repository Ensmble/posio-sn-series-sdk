<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView style="@style/SectionHeader" android:text="Printer info"/>

        <Button android:id="@+id/btnRefresh" style="@style/Cell" android:text="Refresh"/>

        <TextView
            android:id="@+id/info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="12dp"
            android:textIsSelectable="true"
            android:fontFamily="monospace"
            android:text="(tap Refresh)"/>

        <TextView style="@style/SectionHeader" android:text="Print density"/>

        <EditText
            android:id="@+id/etDensity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Density (e.g. 120)"
            android:importantForAutofill="no"
            android:inputType="number"/>
        <Button android:id="@+id/btnSetDensity" style="@style/Cell" android:text="Set density"/>

    </LinearLayout>
</ScrollView>
